spring:
  application:
    name: contact-aggregator
  threads:
    virtual:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health

kenect:
  labs:
    base-url: "https://candidate-challenge-api-489237493095.us-central1.run.app"
    auth-token: ${KENECT_AUTH_TOKEN:J7ybt6jv6pdJ4gyQP9gNonsY}
